# Исправления в системе бронирования

## ✅ 1. Парсинг паспортов (passport_parser.py)

### Проблема:
- OCR распознавал MRZ (нижнюю зону паспорта) на **кириллице вместо латиницы**
- Пример: "ТОМАМО\< <ВОГАТ" вместо "TUMANOV<<BOLAT"
- Имена и фамилии выходили на кириллице или неправильно

### Решение:
1. **Автоматическая транслитерация MRZ**:
   - Парсер теперь находит MRZ даже если OCR распознал его на кириллице
   - Автоматически транслитерирует кириллицу в латиницу
   - Поддерживает разные варианты шевронов: `<<`, `< <`, `\< <`

2. **Улучшенная приоритизация**:
   - Латиница из текстовых полей > MRZ (транслитерированный) > Кириллица из текста
   - Вся кириллица в итоге транслитерируется

3. **Автосохранение OCR текста**:
   - Каждый паспорт создает файл `.ocr_text.txt` для отладки
   - Debug режим для первых 3 паспортов

### Файлы:
- [passport_parser.py](bull_project/bull_bot/core/parsers/passport_parser.py)
- [booking_handlers.py](bull_project/bull_bot/handlers/booking_handlers.py) (строки 164-201)

---

## ✅ 2. Перенос брони

### Проблема:
- При переносе брони появлялась ошибка "Нет паломников"
- Данные паспорта терялись после выбора таблицы/пакета
- Обработчики не срабатывали

### Причина:
В [history_handlers.py:78](bull_project/bull_bot/handlers/history_handlers.py#L78) устанавливалось состояние `BookingFlow.choosing_table`, но обработчики были только для `CareFlow.choosing_table` - **разные FSM**!

### Решение:
1. **Добавлены обработчики для BookingFlow**:
   - `booking_sel_table` - выбор таблицы
   - `booking_sel_date` - выбор даты
   - `booking_sel_pkg` - выбор пакета

2. **Добавлены состояния**:
   ```python
   class BookingFlow:
       choosing_table = State()
       choosing_date = State()
       choosing_pkg = State()
   ```

3. **Логика переноса**:
   - При выборе пакета проверяется флаг `is_reschedule`
   - Если это перенос - сразу отправляет в веб-форму
   - Данные паспорта сохраняются через весь процесс

4. **Добавлено логирование**:
   - Видно количество паломников
   - Видно имена и данные
   - Легко отлаживать проблемы

### Файлы:
- [booking_handlers.py](bull_project/bull_bot/handlers/booking_handlers.py) (строки 40-43, 583-661)
- [history_handlers.py](bull_project/bull_bot/handlers/history_handlers.py) (строки 25-79)

---

## 📋 Как тестировать:

### Парсинг паспортов:
1. Загрузите паспорт через бота
2. Проверьте логи - должны быть латинские имена
3. Проверьте файл `.ocr_text.txt` - там полный OCR текст

### Перенос брони:
1. Откройте историю броней
2. Выберите бронь для переноса
3. Выберите новую таблицу/дату/пакет
4. Откроется веб-форма с данными паспорта
5. Проверьте логи - должны быть данные паломника

---

## 🔧 Дополнительно:

### Логи при парсинге:
```
============================================================
🔍 НАЧАЛО ПАРСИНГА
============================================================
📄 OCR TEXT START========================================
[полный текст OCR]
========================================== OCR TEXT END

✅ ИИН: 640422302688
✅ Дата рождения из ИИН: 22.04.1964
✅ Пол (текст): M
✅ Номер документа: N17792080
✅ Фамилия: TUMANOV
✅ Найдено имя: BOLAT

📍 Найден MRZ паттерн:
   Исходная фамилия: 'ТОМАМО'
   Исходное имя:     'ВОГАТ'
   ⚠️  Фамилия на кириллице, транслитерируем...
   ✅ После транслитерации: 'TOMAMO'
   ⚠️  Имя на кириллице, транслитерируем...
   ✅ После транслитерации: 'VOGAT'
```

### Логи при переносе:
```
============================================================
📤 ОТПРАВКА В WEBAPP (send_webapp_link)
============================================================
  is_reschedule: True
  old_booking_id: 123
  Количество паломников: 1
  pilgrims_list present: True
  Паломник 1: TUMANOV BOLAT
============================================================
```

---

## 🎯 Итог:

✅ **Парсинг паспортов**: Имена ВСЕГДА на латинице
✅ **Перенос брони**: Работает полностью
✅ **OCR текст**: Сохраняется для отладки
✅ **Логирование**: Подробное для отладки

---

Дата исправлений: 2025-12-25
