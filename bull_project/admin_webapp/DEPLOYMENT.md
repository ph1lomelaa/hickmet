# 🚀 Установка Админ WebApp

## 📋 Что было добавлено:

### 1. **Новые функции аналитики** (`database/requests.py`)
- `get_full_analytics()` - полная статистика за период
- `get_manager_detailed_stats()` - детальная статистика по менеджеру
- `search_packages_by_date()` - поиск пакетов по дате

### 2. **API Endpoints** (`core/api_server.py`)
- `GET /api/admin/analytics` - общая аналитика
- `GET /api/admin/managers` - список менеджеров
- `GET /api/admin/manager/{id}` - статистика менеджера
- `GET /api/admin/search/packages` - поиск пакетов

### 3. **WebApp Интерфейс** (`admin_webapp/admin-panel.html`)
- Красивая современная админ-панель
- Графики продаж (Chart.js)
- Рейтинг менеджеров
- Поиск пакетов
- Интеграция с Telegram WebApp API

### 4. **Обновленные хендлеры** (`handlers/admin_handlers.py`)
- Новая кнопка "Админ Панель (WebApp)"
- Интеграция с WebApp

---

## 🔧 Установка (3 шага):

### ШАГ 1: Разместите HTML файл

**Вариант А: GitHub Pages (Рекомендуется)**

1. Создайте репозиторий на GitHub (например, `admin-panel`)
2. Загрузите файл `admin-panel.html` в корень репозитория
3. Зайдите в Settings → Pages
4. Выберите ветку `main` и папку `/root`
5. Сохраните - получите URL типа: `https://your-username.github.io/admin-panel/admin-panel.html`

**Вариант Б: Netlify/Vercel**

1. Перетащите файл на Netlify.com
2. Получите URL автоматически

**Вариант В: Свой сервер**

```bash
# Положите файл на сервер и настройте Nginx
location /admin {
    alias /path/to/admin-panel.html;
}
```

### ШАГ 2: Обновите URL в коде

Откройте файл `bull_project/bull_bot/handlers/admin_handlers.py`:

```python
# Строка 19 - замените на ваш URL
ADMIN_WEBAPP_URL = "https://your-username.github.io/admin-panel/admin-panel.html"
```

### ШАГ 3: Запустите API сервер

**В отдельном терминале:**

```bash
cd bull_project
python -m bull_bot.core.api_server
```

Сервер запустится на `http://127.0.0.1:8000`

**Важно:** API должен быть доступен для WebApp. Если хостите на сервере:

1. Измените `API_BASE` в `admin-panel.html` (строка 418):
```javascript
const API_BASE = 'http://YOUR_SERVER_IP:8000';
```

2. Настройте CORS в `api_server.py` (уже настроено на `allow_origins=["*"]`)

---

## ✅ Проверка работы:

1. Запустите бота: `python -m bull_bot.main`
2. Запустите API: `python -m bull_bot.core.api_server`
3. Зайдите в бота под админом
4. Нажмите "🌐 Админ Панель (WebApp)"
5. Откроется красивый интерфейс!

---

## 📊 Функции Админ Панели:

### 1. **Общая Статистика**
- Выбор периода (сегодня/неделя/месяц/свой)
- Карточки с метриками (всего продаж, отменено, % отмен)
- График динамики продаж
- ТОП-10 пакетов (с кнопкой "Показать все")
- Рейтинг менеджеров

### 2. **Менеджеры**
- Список всех менеджеров
- Поиск по имени
- Клик на менеджера → детальная статистика:
  - Всего/активных/отмененных броней
  - ТОП пакетов менеджера
  - Последние 10 броней

### 3. **Поиск Пакетов**
- Ввод даты (ДД.ММ)
- Показывает все пакеты с этой датой
- Количество продаж по каждому

### 4. **Запросы 4U**
- Пока остается старая функциональность
- Будет улучшена в следующей версии

---

## 🎨 Кастомизация:

### Изменить цвета:

В `admin-panel.html` найдите CSS-переменные:

```css
/* Основной градиент */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Измените на свой: */
background: linear-gradient(135deg, #YOUR_COLOR1 0%, #YOUR_COLOR2 100%);
```

### Добавить свою метрику:

1. Добавьте функцию в `database/requests.py`
2. Добавьте endpoint в `api_server.py`
3. Вызовите в JavaScript `admin-panel.html`

---

## 🐛 Troubleshooting:

### Проблема: WebApp не открывается
**Решение:** Убедитесь, что:
- HTML файл доступен по HTTPS (HTTP не работает в Telegram WebApp)
- URL правильно указан в `admin_handlers.py`

### Проблема: Ошибка CORS
**Решение:**
```python
# В api_server.py уже настроено:
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # Для продакшена укажите конкретный домен
    ...
)
```

### Проблема: API недоступен
**Решение:**
- Запустите API сервер
- Проверьте, что порт 8000 не занят
- Для продакшена используйте nginx reverse proxy

---

## 🚀 Деплой на Production:

### 1. API Server (FastAPI)

**С помощью systemd:**

```bash
# /etc/systemd/system/bull-api.service
[Unit]
Description=Bull Bot API Server
After=network.target

[Service]
User=youruser
WorkingDirectory=/path/to/bull_project
ExecStart=/path/to/.venv/bin/python -m bull_bot.core.api_server
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable bull-api
sudo systemctl start bull-api
```

### 2. Nginx Reverse Proxy:

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 3. SSL (Обязательно для WebApp):

```bash
sudo certbot --nginx -d api.yourdomain.com
```

---

## 📈 Что можно улучшить дальше:

1. ✅ **Фильтры** - по региону, городу вылета, источнику
2. ✅ **Экспорт** - скачать отчет в Excel/PDF
3. ✅ **Уведомления** - push-уведомления о новых заявках
4. ✅ **Детали брони** - клик на бронь → полная информация
5. ✅ **Редактирование** - изменить бронь прямо из WebApp
6. ✅ **Чат** - внутренний чат между админами и менеджерами
7. ✅ **Календарь** - визуализация заполненности по датам

---

## 🎯 Результат:

Теперь у вас есть **современная админ-панель** с:

- 📊 Полной аналитикой
- 👥 Рейтингом менеджеров
- 📈 Графиками продаж
- 🔍 Умным поиском
- 🎨 Красивым дизайном
- 📱 Работой в Telegram WebApp

---

## 💬 Поддержка:

Если возникли вопросы - пишите!

**Made with ❤️ by Claude & muslimakosmagambetova**
