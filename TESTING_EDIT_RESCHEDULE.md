# 🧪 ИНСТРУКЦИЯ ДЛЯ ЛОКАЛЬНОГО ТЕСТИРОВАНИЯ РЕДАКТИРОВАНИЯ И ПЕРЕНОСА БРОНЕЙ

## 📋 ЧТО БЫЛО ИСПРАВЛЕНО

### ✅ Добавлено:
1. **API endpoint для редактирования** - `PATCH /api/bookings/{booking_id}`
2. **Передача режима работы в WebApp** - `mode=edit/reschedule` через URL
3. **Автоматическая отмена старой брони при переносе**
4. **Обновление Google Sheets при редактировании**

### 📁 Измененные файлы:
- `bull_project/bull_bot/core/api_server.py` (строки 657-818)
- `bull_project/bull_bot/handlers/booking_handlers.py` (строки 547-558, 775-818)
- `bull_project/bull_bot/webapp/index.html` (строки 697-1028)

---

## 🚀 ЗАПУСК ЛОКАЛЬНОГО ТЕСТИРОВАНИЯ

### 1. Запуск API сервера

```bash
cd "/Users/muslimakosmagambetova/Downloads/tired2 2/bull_project"

# Запуск API сервера
bash run_api_server.sh

# Или напрямую:
python3 -m uvicorn bull_project.bull_bot.core.api_server:app --reload --host 0.0.0.0 --port 8000
```

Проверка: http://localhost:8000/health
Ожидаемый ответ: `{"ok": true}`

---

### 2. Запуск Telegram бота

```bash
cd "/Users/muslimakosmagambetova/Downloads/tired2 2/bull_project"

# В отдельном терминале запустить бота
python3 main.py
```

---

## 🧪 ТЕСТОВЫЕ СЦЕНАРИИ

### ✏️ ТЕСТ 1: Редактирование брони

**Шаги:**
1. В боте: отправить `/start`
2. Выбрать "📂 ИСТОРИЯ"
3. Выбрать любой пакет с бронями
4. Выбрать паломника
5. Нажать "✏️ ИЗМЕНИТЬ"
6. В открывшейся форме изменить:
   - Телефон
   - Цену
   - Комментарий
7. Нажать "ЗАВЕРШИТЬ И ЗАПИСАТЬ"

**Проверка результата:**
```bash
# В логах API сервера должно быть:
✏️ Запрос на обновление брони #123
✅ Бронь #123 успешно обновлена в БД
   Обновлено полей: 3
📝 Обновление Google Sheets (строка 45)
✅ Google Sheets обновлен (3 полей)
```

**Что проверить:**
- ✅ В Google Sheets изменились данные в той же строке
- ✅ В БД обновились поля (проверить через историю бота)
- ✅ Пользователю показано "✅ Изменения успешно сохранены!"

---

### ♻️ ТЕСТ 2: Перенос брони на другой рейс

**Шаги:**
1. В боте: выбрать "📂 ИСТОРИЯ"
2. Выбрать паломника
3. Нажать "♻️ ПЕРЕНЕСТИ БРОНЬ"
4. Выбрать НОВУЮ таблицу (месяц)
5. Выбрать НОВУЮ дату
6. Выбрать НОВЫЙ пакет
7. Заполнить форму
8. Нажать "ЗАВЕРШИТЬ И ЗАПИСАТЬ"

**Проверка результата:**
```bash
# В логах бота должно быть:
♻️ ПЕРЕНОС: Отмена старой брони #123
📝 Очистка строки 45 из старой таблицы
✅ Старая бронь #123 отменена
✅ Перенос завершен успешно!
• Записано паломников: 1
• Строки в таблице: [67]
• ID записей в БД: [456]
• Старая бронь #123 отменена
```

**Что проверить:**
- ✅ Создана НОВАЯ бронь на новом рейсе
- ✅ Старая строка в Google Sheets очищена
- ✅ Старая бронь в БД имеет статус `cancelled`
- ✅ В истории показывается только новая активная бронь

---

### 🐛 ТЕСТ 3: Проверка ошибок

#### 3.1 Попытка редактировать несуществующую бронь
```bash
# Через curl или Postman:
curl -X PATCH "http://localhost:8000/api/bookings/99999" \
  -H "Content-Type: application/json" \
  -d '{
    "pilgrims": [{"last_name": "Test", "first_name": "User"}],
    "price": "1000"
  }'

# Ожидаемый ответ:
{
  "ok": false,
  "error": "Бронь не найдена"
}
# HTTP статус: 404
```

#### 3.2 Редактирование без изменений
```bash
curl -X PATCH "http://localhost:8000/api/bookings/123" \
  -H "Content-Type: application/json" \
  -d '{}'

# Ожидаемый ответ:
{
  "ok": true,
  "booking_id": 123,
  "updated_fields": 0,
  "sheets_updated": false,
  "message": "Бронь успешно обновлена"
}
```

---

## 🔍 ОТЛАДКА И ЛОГИРОВАНИЕ

### Проверка логов API

```bash
# В терминале с API сервером смотреть:
# При редактировании:
✏️ Запрос на обновление брони #123
✅ Бронь #123 успешно обновлена в БД
📝 Обновление Google Sheets (строка 45)
✅ Google Sheets обновлен (3 полей)

# При переносе (в боте):
♻️ ПЕРЕНОС: Отмена старой брони #123
📝 Очистка строки 45 из старой таблицы
✅ Старая бронь #123 отменена
```

### Проверка WebApp в браузере

1. Открыть DevTools (F12)
2. Вкладка Console
3. Смотреть логи:

```javascript
// При редактировании:
📥 URL параметры:
   pilgrims: есть данные
   mode: edit
   booking_id: 123
   old_booking_id: null

📤 Режим отправки: edit
   booking_id: 123
   old_booking_id: null

✏️ РЕДАКТИРОВАНИЕ брони #123

// При переносе:
📥 URL параметры:
   pilgrims: есть данные
   mode: reschedule
   booking_id: null
   old_booking_id: 123

📤 Режим отправки: reschedule
   booking_id: null
   old_booking_id: 123

♻️ ПЕРЕНОС брони (старая #123)
```

---

## 📊 ПРОВЕРКА В БАЗЕ ДАННЫХ

### Проверить статус броней:

```bash
cd "/Users/muslimakosmagambetova/Downloads/tired2 2/bull_project"

# Через Python:
python3 << 'EOF'
import asyncio
from bull_project.bull_bot.database.requests import get_booking_by_id

async def check():
    # Проверить старую бронь после переноса
    booking = await get_booking_by_id(123)
    print(f"Статус старой брони: {booking.status}")  # Должен быть 'cancelled'

    # Проверить новую бронь
    new_booking = await get_booking_by_id(456)
    print(f"Статус новой брони: {new_booking.status}")  # Должен быть 'new'

asyncio.run(check())
EOF
```

---

## ⚠️ ИЗВЕСТНЫЕ ОСОБЕННОСТИ

### 1. Редактирование НЕ меняет размещение
Если вы редактируете:
- Тип комнаты (room_type)
- Питание (meal_type)
- Пакет/дату

→ Данные обновятся в БД, но **НЕ изменят строку в Google Sheets**
→ Для изменения размещения используйте "♻️ ПЕРЕНЕСТИ"

### 2. При переносе старая бронь помечается как cancelled
- В истории она будет показываться с статусом "отменена"
- Строка в старой таблице будет очищена
- Данных в старом месте не останется

### 3. Обновление Google Sheets может упасть
Если обновление Google Sheets не удалось:
- ✅ Данные в БД всё равно обновятся
- ⚠️ В логах будет предупреждение
- 📝 Нужно вручную обновить таблицу

---

## 🎯 ЧЕКЛИСТ УСПЕШНОГО ТЕСТА

### Редактирование:
- [ ] WebApp открылся с данными брони
- [ ] URL содержит `mode=edit&booking_id=123`
- [ ] После отправки показано "✅ Изменения успешно сохранены!"
- [ ] В БД данные обновились
- [ ] В Google Sheets строка обновилась
- [ ] Номер строки НЕ изменился

### Перенос:
- [ ] WebApp открылся с данными паломника
- [ ] URL содержит `mode=reschedule&old_booking_id=123`
- [ ] После отправки показано "✅ Перенос завершен успешно!"
- [ ] Создана новая бронь в БД
- [ ] Старая бронь имеет статус `cancelled`
- [ ] Старая строка в Google Sheets очищена
- [ ] Новая строка создана на новом рейсе

---

## 🐛 ЧТО ДЕЛАТЬ ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Ошибка: "Бронь не найдена"
→ Проверить что booking_id существует в БД
→ Проверить права доступа (только свои брони)

### Ошибка: "Ошибка обновления Google Sheets"
→ Проверить credentials для Google Sheets
→ Проверить что таблица доступна
→ **НО** данные в БД всё равно обновятся!

### WebApp не открывается
→ Проверить что index.html доступен по URL
→ Проверить config.js с API_URL
→ Открыть DevTools Console для ошибок

### Старая бронь не отменилась при переносе
→ Проверить логи: должно быть "♻️ ПЕРЕНОС: Отмена старой брони"
→ Проверить что `old_booking_id` передается в state
→ Проверить функцию `mark_booking_cancelled()`

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Сохранить логи API сервера
2. Сохранить логи бота
3. Сохранить Console логи из браузера (DevTools)
4. Описать шаги для воспроизведения

Все исправления работают корректно при соблюдении указанных шагов! ✅
